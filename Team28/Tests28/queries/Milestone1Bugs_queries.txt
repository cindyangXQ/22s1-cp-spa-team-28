1 - PQL clause with 2 identical synonyms (Follows)
stmt s;
Select s such that Follows(s,s)
none
5000
1a - PQL clause with 2 different synonyms (Follows)
stmt s1, s2;
Select s1 such that Follows(s1,s2)
1, 2, 6
5000
2 - PQL clause with 2 identical synonyms (Parent)
stmt s;
Select s such that Parent(s,s)
none
5000
2a - PQL clause with 2 different synonyms (Parent)
stmt s1, s2;
Select s1 such that Parent(s1,s2)
2, 4
5000
3 - Variables in while clause are captured in Uses relationship
variable v;
Select v such that Uses(2,v)
a1, a2, a3
5000
3a - Variables in while clause are captured in Uses relationship
variable v;
Select v such that Uses(11,v)
b1, b2, b3, b4, c1, c2, c3, d1, d2, d3, d4
5000
3b - Variables in while clause are captured in Uses relationship
variable v;
Select v such that Uses(12,v)
b3, b4, c1, c2, c3, d1, d2, d3, d4
5000
3c - Variables in while clause are captured in Uses relationship
variable v;
Select v such that Uses(13,v)
c1, c2, c3, d1, d2, d3, d4
5000
3d - Variables in while clause are captured in Uses relationship
variable v;
Select v such that Uses(14,v)
d1, d2
5000
3e- Variables in while clause are captured in Uses relationship
variable v;
Select v such that Uses(16,v)
d3, d4
5000
3f- Variables in while clause are captured in Uses relationship
stmt s;
Select s such that Uses(s,"d3")
11, 12, 13, 16
5000
4 - Variables in if clause are captured in Uses relationship
variable v;
Select v such that Uses(4,v)
a1, a4, a5, a6, a7, a8, a9, x, y, z
5000
4a - Variables in if clause are captured in Uses relationship
stmt s;
Select s such that Uses(s,   "a4"  )
4
5000
4b - Variables in if clause are captured in Uses relationship
stmt s;
Select s such that Uses(s,   "a6"  )
4, 6
5000
4c - Variables in if clause are captured in Uses relationship
variable v;
Select v such that Uses(6, v)
a6, a7, a8, y, z
5000
5 - Handle white space in quotation marks for pattern clause
assign a; variable v;
Select v pattern a(v, _"1 "_)
x
5000
6 - Invalid expression for pattern clause (To confirm)
assign a; variable v;
Select v pattern a(v, _"1) "_)
SyntaxError
5000
7a - Semantically invalid query on usage of c-syn in Uses
stmt s; constant c;
Select s such that Uses(s, c)
SemanticError
5000
7b - Semantically invalid query on usage of c-syn in patt
assign a; constant c;
Select a pattern a(c, _)
SemanticError
5000
8 - Semantically invalid query on usage of s-syn in patt
assign a; stmt s;
Select s pattern a(s, _)
SemanticError
5000