#pragma once

/*
 * Enumerates the different design entity.
 */
enum class DesignEntity
{
    PROCEDURE,
	STATEMENT,
    READ,
    PRINT,
    ASSIGN,
    CALL,
    WHILE,
    IF,
	VARIABLE,
    CONSTANT,
};